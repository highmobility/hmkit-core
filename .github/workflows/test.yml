name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Compile unit test's
      run: unit_test/make
    - name: Run unit test's
      run: unit_test/hmservice
